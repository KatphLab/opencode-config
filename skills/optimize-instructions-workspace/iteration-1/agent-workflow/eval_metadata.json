{
  "eval_id": 1,
  "eval_name": "agent-workflow",
  "prompt": "Tighten this verbose agent workflow doc without changing its workflow. Keep the safety checks and output format intact, and update the file itself rather than giving me notes.",
  "assertions": [
    "Preserves the exact required response headings: Verdict, Missing information, Risks, Recommended changes",
    "Preserves the missing-plan behavior and does not invent a deployment plan",
    "Preserves safety checks for production/high-risk deployment changes and asks clarifying questions before approval",
    "Preserves the boundary that the agent reviews but does not execute deployments",
    "Output is shorter than the input fixture"
  ]
}
